namespace app_testing_TDD
{
    internal class TicketResponse
    {   
        //an array of tickets (from example, not rly applicable here because i need to return a string?
        public TicketType[] Tickets { get; set; }
    }
}